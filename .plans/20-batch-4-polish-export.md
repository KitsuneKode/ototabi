# Batch 4 — Landing, PRODUCT, Editor Foundation

**Goal:** Plan 16 landing remainder, staged PRODUCT copy, timeline-lite + TranscriptEditor basics, export worker shell.

## Tasks

- [x] Landing comparison strip + CRT waveform motion
- [x] `PRODUCT.md` staged creator suite section
- [x] `TranscriptEditor` + `TimelineLite` components
- [x] Export render processor on Railway worker (`export` queue)

## Verification

```bash
bun fmt && bun lint && bun typecheck && bun run test
```
