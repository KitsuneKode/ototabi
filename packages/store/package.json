{
  "name": "@ototabi/store",
  "private": true,
  "type": "module",
  "module": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "db:generate": "bun run prisma generate",
    "db:migrate": "bun run prisma migrate deploy",
    "db:seed": "bun run src/scripts/seed.ts",
    "db:studio": "bun run prisma studio",
    "db:deploy": "bun run prisma deploy",
    "db:format": "bun run prisma format",
    "db:format:check": "bun run prisma format --check"
  },
  "dependencies": {
    "@prisma/adapter-pg": "catalog:",
    "@prisma/client": "catalog:",
    "pg": "catalog:",
    "prisma": "catalog:"
  },
  "devDependencies": {
    "@ototabi/typescript-config": "workspace:*",
    "@types/bun": "catalog:",
    "dotenv": "catalog:",
    "typescript": "catalog:"
  }
}
