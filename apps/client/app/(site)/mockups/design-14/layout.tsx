import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Design 14 — Bold Brutalist",
  description: "High-contrast brutalist studio interface.",
};

export default function Design14Layout({ children }: { children: React.ReactNode }) {
  return children;
}
