import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Design 12 — Midnight Glow",
  description: "Dark neon glow accents on midnight surfaces.",
};

export default function Design12Layout({ children }: { children: React.ReactNode }) {
  return children;
}
