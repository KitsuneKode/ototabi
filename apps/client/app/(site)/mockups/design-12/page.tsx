import type { Metadata } from "next";

import Design12ClientPage from "./design-12-client";

export const metadata: Metadata = {
  title: "Design 12 — Midnight Glow",
  description: "Dark neon glow accents on midnight surfaces.",
};

export default function Design12Page() {
  return <Design12ClientPage />;
}
